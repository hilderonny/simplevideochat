# simplevideochat
This application  shows how to make an easy video chat within a local wireless network between Android devices. There is no need for servers, neither internal LAN servers nor external servers.

Use the Core class to configure socket ports and timeouts.
